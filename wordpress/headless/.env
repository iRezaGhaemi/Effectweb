# تنظیمات اتصال به وردپرس (برای Docker / docker-compose)
# بعد از ویرایش، دوباره build کنید:
#   docker compose up -d --build

# آدرس سایت وردپرس (backend). بدون / در انتها.
NEXT_PUBLIC_WP_URL=

# (در صورت استفاده از ووکامرس) کلیدهای REST
NEXT_PUBLIC_WC_CONSUMER_KEY=
NEXT_PUBLIC_WC_CONSUMER_SECRET=
